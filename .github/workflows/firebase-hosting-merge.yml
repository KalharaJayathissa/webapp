# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_KALHARAJ }}
          channelId: live
          projectId: kalharaj

      - name: Deploy to Firebase Hosting 2
        run: |
          echo "Deploying to Firebase Hosting..."
          firebase deploy --only hosting --project kalharaj
        env:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_KALHARAJAYATHISSA }}
          channelId: live
          projectId: kalharajayathissa
          }
